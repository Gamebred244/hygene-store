# suppress inspection "UnusedProperty"
spring.application.name=store

# suppress inspection "UnusedProperty"
app.page-size=10

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/store}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:root}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
server.port=${PORT:8081}

app.jwt.secret=ZmFrZXNlY3JldGtleWZvcnN0b3JlYXBwMTIzNDU2Nzg5MDEyMzQ=
app.jwt.expiration-ms=86400000
app.jwt.cookie-name=store_auth
app.jwt.remember-ms=604800000
app.frontend.base-url=http://localhost:4200
app.reset.expiration-mins=30
app.paypal.client-id=${PAYPAL_CLIENT_ID:}
app.paypal.secret=${PAYPAL_SECRET:}
app.paypal.base-url=https://api-m.sandbox.paypal.com

spring.mail.host=
spring.mail.port=587
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# OAuth2: fill these to enable Google/Facebook login
# spring.security.oauth2.client.registration.google.client-id=
# spring.security.oauth2.client.registration.google.client-secret=
# spring.security.oauth2.client.registration.google.scope=openid,profile,email
# spring.security.oauth2.client.registration.facebook.client-id=
# spring.security.oauth2.client.registration.facebook.client-secret=
# spring.security.oauth2.client.registration.facebook.scope=email,public_profile

app.db.admin.url=${DB_ADMIN_URL:}
app.db.admin.username=${DB_ADMIN_USER:}
app.db.admin.password=${DB_ADMIN_PASS:}
app.db.name=${DB_NAME:}
